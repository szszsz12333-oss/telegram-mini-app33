// For GitHub Pages, set the HTTPS URL of your backend, for example:
// window.APP_CONFIG = { apiBaseUrl: 'https://api.example.com' };
// Leave empty only when the frontend and server are hosted on the same domain.
window.APP_CONFIG = { apiBaseUrl: '' };
